{
    "showtrack": {
        "type": "checkbox",
        "default": true,
        "description": "Show now playing information on the panel"
    },
    "showalbum": {
        "type": "checkbox",
        "default": true,
        "description": "Show the album art as the icon on the panel"
    },
    "truncatetext": {
        "type": "spinbutton",
        "default": 30,
        "min": 10,
        "max": 255,
        "units": "characters",
        "step": 1,
        "description": "Truncate the track title on the panel to "
    }
}
